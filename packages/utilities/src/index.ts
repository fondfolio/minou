import {css} from 'styled-components';

const utilities = {};

export const reset = css`
  appearance: none;
  border: none;
  background: none;
  font-size: 100%;
  font-family: inherit;
`;

export default utilities;
